# Auth & secrets
.env*
!.env.example
*.key
*.pem

# Assistant runtime (legacy — kept out of the repo even though the agent files are gone)
memory/
MEMORY.md

# Runtime data (SQLite DB, JSONL backup, frames, logs, alert state, recap videos)
data/

# AirGradient logger runtime artifacts
airgradient-logger/data/
airgradient-logger/logs/
airgradient-logger/venv/

# baby-report (sibling package) venv
report/venv/

# Model weights / training output
pipeline/models/
pipeline/output/
yolov8n.pt
*.pt
*.onnx

# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/
build/
dist/

# OS
.DS_Store
Thumbs.db

# Temp files from media/screenshots (PWA icons + README hero are explicit exceptions below)
*.jpg
*.png
*.gif

# PWA app icons are checked in (regenerated from icon.svg via qlmanage)
!dashboard/static/icons/*.png

# README hero image
!docs/*.png

# Claude Code (keep settings.json for hooks, ignore local overrides)
.claude/*
!.claude/settings.json
